#
# Format:
#
# <stage name>:
#   app: <Heroku app name>
#   stack: <Heroku stack, optional>
#   tag: <git tag pattern, optional>
#   repo: <git repository, optional>
#   config:
#     - <Heroku config:var name>: <Heroku config:var value>
#

production:
  app: miluno
  stack: cedar
  config: &default
    BUNDLE_WITHOUT: "development:test"
    HOST_URL: "http://miluno.herokuapp.com"

testing:
  app: miluno-testing
  stack: cedar
  config: &default
    BUNDLE_WITHOUT: "development:test"
    HOST_URL: "http://miluno-testing.herokuapp.com"
